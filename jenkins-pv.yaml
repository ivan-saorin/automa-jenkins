apiVersion: v1
kind: PersistentVolume
metadata:
  name: jenkins-pv
  namespace: kube-system
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/jenkins_home"
